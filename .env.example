# YouTubeMint — Showcase Build
# Copy to .env.local. No system binaries required — this is the demo build.

# Public site URL (update before deploying)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# No YT_DLP_PATH, TEMP_DIR, or server credentials needed in showcase mode.
# Those environment variables belong to the private production repository.
